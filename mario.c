#include <cs50.h>
#include <stdio.h>

int main(void)

string s = get_string ("Vvedy");
{
for (int x = 0; x < h; x++)
    {
    for (int y = 0; y < h; y++)
      {
        if (x + y < h - 1)
        printf(" ");
        else 
        printf("#");
      }
    }
}